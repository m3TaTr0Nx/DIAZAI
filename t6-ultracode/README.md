# DIAZAI T6 Ultracode MCP Foundry V1

This branch contains the core source snapshot for the DIAZAI T6 Ultracode production suite.

## Implemented surfaces

- deterministic `Z9^3` 729-state address kernel;
- six-trit positional codec and three exhaustive 27x27 charts;
- corrected X/Y/Z superplanes `k-i=m(j-i)+q (mod 9)` with slopes 2, 5 and 8;
- exact `p/(2^a 3^b)` mixed-radix rationals;
- BTMR32 binary/ternary research word with orientation, domain, class, exponent, 12 balanced trits and checksum;
- complete deterministic Turing 7-tuple simulator, orientation-aware ternary tape and bytecode lowering;
- OASIS/STAR to DIAZAI causal event transcription with secret/PII filtering, deterministic state routing and approval-gated reverse commands;
- hash-chained receipts, replay and first-divergence localization;
- Python FastAPI and Node.js HTTP/MCP runtimes;
- a 161-tool / 14-skill MCP foundry with executable examples and cross-runtime validation;
- C and C# non-blocking STARAPIClient sidecar patterns;
- Canon V5, benchmark suite, schemas and an offline interactive superconsole.

## Truth boundary

The software, finite arithmetic, codecs and tests are executable. Physical-drive, antiverse, eversion-thrust and similar historical descriptions are retained as hypotheses only and are not validated propulsion or energy results. IP candidate records are invention disclosures, not legal conclusions.

## OASIS integration boundary

OASIS retains game and business semantics. Game callbacks remain small; OGLib remains shared game integration; STARAPIClient remains the Web4/Web5 HTTP, auth, cache, queue, inventory, quest and NFT client. DIAZAI attaches after operation completion as a bounded, non-blocking evidence and control sidecar.

## Local validation in the release package

- 15 Python tests passed;
- 8 Node tests passed;
- 159 non-recursive foundry tools executed successfully in each runtime;
- 729-state and three-chart exhaustive validation passed;
- Python/Node canonical event hashes and state addresses matched;
- Canon V5 hash chain passed;
- C adapter compiled and passed smoke execution;
- synthetic local benchmarks were generated for 50,000 events and 20,000 tool calls.

The downloadable release archive remains the authoritative complete artifact because it also contains the 161-tool manifest, 14 skill packages, interactive HTML, DOCX/PDF technical book, XLSX inventory, reports and deployment bundles.